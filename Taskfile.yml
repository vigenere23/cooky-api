version: '3'

includes:
  app:
    taskfile: ./app
    dir: ./app

  ui:
    taskfile: ./ui
    dir: ./ui

  db:
    taskfile: ./Taskfile_db.yml
    dir: .

tasks:
  default: source ./art.sh

  destroy:
    desc: Delete images and containers from docker compose
    cmds:
      - docker-compose down

  start:
    desc: Start all containers from docker compose
    cmds:
      - task: db:start
      - task: app:start
      - task: ui:start

  logs:
    desc: Attach to docker compose logs
    cmds:
      - docker-compose logs -f

  start+logs:
    desc: Alias for 'start' followed by 'logs'
    cmds:
      - task: start
      - task: logs
